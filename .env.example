# ---------------------------------------------------------------------------
# Root infrastructure config (consumed by docker-compose.yml).
# Copy to `.env` to override the local-development defaults.
# ---------------------------------------------------------------------------
POSTGRES_USER=saas
POSTGRES_PASSWORD=saas_dev_password
POSTGRES_DB=saas_boilerplate
POSTGRES_PORT=5432
